# Backend API URL
# Dev: http://localhost:8000
# Prod: set in the Netlify UI if the frontend ever calls the API directly (currently unused — the demo runs fully client-side)
VITE_BACKEND_URL=http://localhost:8000
