# Test-only dependencies — tests never load TensorFlow or the .h5 model,
# so CI installs stay small (no tensorflow).
fastapi
uvicorn[standard]
python-multipart
python-dotenv
# <5: OpenCV 5.0 removed cv2.CascadeClassifier from the main namespace
opencv-python-headless<5
numpy
pydantic>=2
pytest
httpx
