# Path to the trained .h5 model file
MODEL_PATH=../../best_vgg_model.h5

# Comma-separated allowed origins, or * for dev
# Production: set to your Netlify URL (e.g. https://emovision.netlify.app)
CORS_ORIGIN=*

# Server port (Render sets this automatically)
PORT=8000
